package org.holodeck.ebms3.consumers.impl;

//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.jms.core.JmsTemplate;
//import org.springframework.jms.core.MessageCreator;
//import org.springframework.stereotype.Component;

/**
 * @author Hamid Ben Malek
 */
//@Component
public class QueueSender
{
/*    
   private final JmsTemplate jmsTemplate;

   @Autowired
   public QueueSender( final JmsTemplate jmsTemplate )
   {
     this.jmsTemplate = jmsTemplate;
   }

   public void send(final byte[] msg, String queueName)
   {
     jmsTemplate.convertAndSend(queueName, msg);
   }

   public void send(final MessageCreator msg, String queueName)
   {
     jmsTemplate.send(queueName, msg);
   }
 */
}