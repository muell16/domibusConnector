<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>domibusConnectorTest</artifactId>
        <groupId>eu.domibus.connector</groupId>
        <version>4.1.2-RELEASE</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <description>This module contains helper classes for testing logging. Also a example log4j2-test.xml is included.
        With the provided helper classes
        a test can check if a specific message has been logged.
    </description>
    <groupId>eu.domibus.connector.test</groupId>
    <artifactId>domibusConnectorTestLogging</artifactId>


    <dependencies>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- attach log4j2-test.xml as artifact for deployment -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>attach-artifact</goal>
                        </goals>
                        <id>attach-log4j2-test-xml</id>
                        <configuration>
                            <artifacts>
                                <artifact>
                                    <file>src/main/resources/log4j2-test.xml</file>
                                    <type>xml</type>
                                    <classifier>log4j2-test</classifier>
                                </artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>