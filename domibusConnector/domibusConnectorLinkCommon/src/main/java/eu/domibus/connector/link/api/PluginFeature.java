package eu.domibus.connector.link.api;

public enum PluginFeature {

    SUPPORTS_MULTIPLE_PARTNERS, //if the plugin supports multiple link partners within one configuration
    PULL_MODE,
    PUSH_MODE
    ;

}
